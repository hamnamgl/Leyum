# Leyum

Offline AI tutor that classifies why a child is wrong and teaches differently based on that.

This scaffold includes the root app entrypoint, core reasoning modules, prompt templates, local storage folders, validation assets, UI files, technical writeup, demo placeholders, and unit-test stubs described in the project outline.
